# 🚀 每日自动提交完整指南

这个脚本系统可以让你的 GitHub 仓库每天都有提交记录，即使没有代码更新也会自动更新 `aboutme.md` 的日期。

## 📦 脚本列表

### 核心脚本

| 脚本 | 用途 | 推荐场景 |
|------|------|----------|
| `daily-auto-commit.sh` | 每日自动提交主脚本 | 核心功能 |
| `setup-daily-cron.sh` | 设置定时任务（Cron） | 服务器/CI 环境 |
| `start-daemon.sh` | 启动守护进程 | 个人电脑/开发环境 |
| `stop-daemon.sh` | 停止守护进程 | 管理守护进程 |
| `test-daily-commit.sh` | 测试脚本功能 | 测试验证 |

## 🎯 快速开始

### 方案一：使用 Cron 定时任务（推荐用于服务器）

**1. 设置定时任务（每天随机时间执行）**
```bash
./setup-daily-cron.sh
```

**2. 查看定时任务**
```bash
crontab -l
```

**3. 查看执行日志**
```bash
tail -f daily-commit.log
```

**4. 删除定时任务**
```bash
crontab -l | grep -v "daily-auto-commit.sh" | crontab -
```

---

### 方案二：使用后台守护进程（推荐用于个人电脑）

**1. 启动守护进程**
```bash
./start-daemon.sh
```

守护进程特点：
- 🎲 每天在 9:00-18:00 之间随机时间执行
- 🔄 自动循环，无需手动重启
- 📝 所有输出记录到 `auto-commit-daemon.log`

**2. 停止守护进程**
```bash
./stop-daemon.sh
```

**3. 查看守护进程状态**
```bash
ps -f | grep start-daemon
```

**4. 查看日志**
```bash
tail -f auto-commit-daemon.log
```

---

### 方案三：手动执行

**直接执行（一次性）**
```bash
./daily-auto-commit.sh
```

**测试模式（查看会做什么但不实际执行）**
```bash
./test-daily-commit.sh
```

---

## 🔍 工作原理

### 智能检测逻辑

1. **检查代码变更**
   - 如果有新的代码变更 → 正常提交代码
   - 如果没有代码变更 → 更新 `aboutme.md` 的日期

2. **智能提交消息**

   **有代码变更时：**
   - 新增文件：`✨ feat: 新增 X 个文件 (2026-01-16)`
   - 修改文档：`📝 docs: 更新文档 (2026-01-16)`
   - 代码优化：`♻️ refactor: 代码优化 (2026-01-16)`
   - 其他：`📦 update: 批量更新 (2026-01-16)`

   **无代码变更时：**
   - 更新日期：`📅 chore: 更新 aboutme.md 日期 (2026-01-16)`

### 自动修改的日期位置

文件：`packages/blogpress/aboutme.md`

```yaml
---
sidebar: false
title: 关于自己
date: 2021-12-11  ← 自动更新这里
cover: /zx-vitepress-blog/mp-code.png
tags:
 - 其它
---
```

---

## ⚙️ 自定义配置

### 修改随机时间范围

编辑 `start-daemon.sh` 或 `setup-daily-cron.sh`：

```bash
# 修改执行时间范围（小时）
MIN_HOUR=9      # 最早执行时间
MAX_HOUR=18     # 最晚执行时间
```

### 修改 aboutme.md 文件路径

编辑 `daily-auto-commit.sh`：

```bash
# 修改为你的实际路径
ABOUTME_FILE="packages/blogpress/aboutme.md"
```

### 修改默认分支

编辑 `daily-auto-commit.sh` 中的 git 命令：

```bash
# 将 main 改为你需要的分支名
git push origin main
```

---

## 📊 日志和监控

### 日志文件位置

- **Cron 模式**：`daily-commit.log`
- **守护进程模式**：`auto-commit-daemon.log`

### 查看实时日志

```bash
# 持续查看最新日志
tail -f daily-commit.log

# 查看最近 100 行
tail -100 daily-commit.log

# 搜索特定内容
grep "✅" daily-commit.log
```

### 日志示例

```
[2026-01-16 13:30:00] === 每日自动提交任务开始 ===
[2026-01-16 13:30:00] 🔍 检查 git 仓库状态...
[2026-01-16 13:30:00] ✅ 没有检测到代码变更
[2026-01-16 13:30:00] 📅 将更新 aboutme.md 的日期为今天: 2026-01-16
[2026-01-16 13:30:01] ✅ 日期更新成功: 2026-01-16
[2026-01-16 13:30:01] ✅ 日期更新提交完成: 📅 chore: 更新 aboutme.md 日期 (2026-01-16)
[2026-01-16 13:30:02] ✅ 推送成功
[2026-01-16 13:30:02] === 每日自动提交任务完成 ===
```

---

## 🛠️ 故障排除

### 常见问题

**Q: 提示 "推送失败"**
```bash
# 检查远程仓库配置
git remote -v

# 测试手动推送
git push origin main
```

**Q: 守护进程不执行**
```bash
# 检查进程是否运行
ps aux | grep start-daemon

# 查看错误日志
tail -f auto-commit-daemon.log
```

**Q: Cron 不执行**
```bash
# 检查 cron 服务状态
sudo systemctl status cron

# 查看 cron 日志
sudo tail -f /var/log/cron
```

**Q: 权限错误**
```bash
# 确保脚本可执行
chmod +x *.sh

# 确保 git 有权限
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
```

### 重置所有配置

```bash
# 停止守护进程
./stop-daemon.sh

# 删除定时任务
crontab -l | grep -v "daily-auto-commit.sh" | crontab -

# 删除日志文件
rm -f *.log *.pid

# 重新设置
./setup-daily-cron.sh
# 或
./start-daemon.sh
```

---

## 🔐 安全注意事项

1. **凭据安全**
   - 确保 git 已配置凭据（推荐使用 SSH 或 credential helper）
   - 不要在脚本中硬编码密码

2. **备份重要文件**
   - 脚本会自动备份 `aboutme.md` 再修改
   - 重要变更前请手动备份

3. **测试环境**
   - 首次使用建议先运行 `./test-daily-commit.sh` 测试
   - 在测试分支上验证功能

---

## 📝 最佳实践

### 1. 定期检查日志
```bash
# 每周检查一次
tail -50 daily-commit.log | grep -E "(❌|ERROR|失败)"
```

### 2. 保持脚本更新
```bash
# 脚本有更新时，重新下载或复制
# 然后重新设置定时任务
```

### 3. 监控提交频率
```bash
# 查看提交历史
git log --oneline --since="7 days ago"
```

### 4. 多项目使用
每个项目都需要单独设置一套脚本。

---

## 🎉 成功示例

你的 GitHub Contributions 将显示：

```
📅 2026-01-15  📅 chore: 更新 aboutme.md 日期 (2026-01-15)
📅 2026-01-14  📝 docs: 更新文档 (2026-01-14)
📅 2026-01-13  ✨ feat: 新增 2 个文件 (2026-01-13)
📅 2026-01-12  📅 chore: 更新 aboutme.md 日期 (2026-01-12)
📅 2026-01-11  ♻️ refactor: 代码优化 (2026-01-11)
```

每天都有提交记录！🎯

---

## 📞 支持

如果遇到问题：

1. 查看日志文件中的错误信息
2. 运行 `./test-daily-commit.sh` 测试功能
3. 检查 git 配置和权限
4. 手动执行一次脚本查看详细输出

---

**祝你的 GitHub Contributions 每天都是满格！🚀**